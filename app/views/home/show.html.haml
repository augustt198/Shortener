.container
  %span.copy{'data-clipboard-text' => 'http://' + request.host + '/' + @link.urlhash} Copy
  %input{value: request.host + '/' + @link.urlhash, :class => "output", :autofocus => true, :readonly => true}
  %a{href: @link.link, :class => "target"}>
    %span> ↳
    = @link.link

.meta
  Visitors:
  = @link.hits
= render partial: 'home/partials/view_graph', locals: {views: @link.view_dates, highest: @highest_view}
